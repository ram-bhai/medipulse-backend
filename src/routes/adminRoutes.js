// adminRoutes.js
const express = require('express');
const router = express.Router();

// const adminController = require('../controllers/adminController');
// const authMiddleware = require('../middlewares/authMiddleware');
// const roleMiddleware = require('../middlewares/roleMiddleware');

// router.get('/dashboard', authMiddleware.authenticate, roleMiddleware.allowOnly(['admin']), adminController.dashboard);
// router.get('/users', authMiddleware.authenticate, roleMiddleware.allowOnly(['admin']), adminController.getAllUsers);

module.exports = router;
    