// doctorRoutes.js
const express = require('express');
const router = express.Router();

// const doctorController = require('../controllers/doctorController');
// const authMiddleware = require('../middlewares/authMiddleware');
// const roleMiddleware = require('../middlewares/roleMiddleware');

// router.get('/profile', authMiddleware.authenticate, roleMiddleware.allowOnly(['doctor']), doctorController.getProfile);
// router.post('/appointments', authMiddleware.authenticate, roleMiddleware.allowOnly(['doctor']), doctorController.getAppointments);

module.exports = router;
